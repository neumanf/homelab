services:
  adguard:
    image: "adguard/adguardhome:latest"
    container_name: adguard
    restart: unless-stopped
    network_mode: host
    volumes:
      - adguard:/opt/adguardhome

volumes:
  adguard:
